/** Material Icons link_off glyph for disconnect actions in the admin UI. */
export function renderDisconnectIconMarkup(className = "disconnect-icon"): string {
  return `<span class="material-icons ${className}" aria-hidden="true">link_off</span>`;
}
